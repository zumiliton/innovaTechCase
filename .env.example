# Select the runtime profile:
# local = local LLM inside Docker
# openai = OpenAI API
COMPOSE_PROFILES=local

# LLM configuration
LLM_PROVIDER=local
LLM_MODEL=

# Only required for OpenAI mode
LLM_API_KEY=

# Application
LOG_LEVEL=INFO