services:
  app:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: arduino-assistant

    ports:
      - "8000:8000"

    env_file:
      - .env

    restart: unless-stopped